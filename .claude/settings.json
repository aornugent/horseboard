{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "npm install --silent && npx playwright install chromium --with-deps > /dev/null 2>&1 || true"
          }
        ]
      }
    ]
  }
}
